# CI temporalmente deshabilitado hasta configurar variables de entorno
# Descomentar cuando estén listos: MONGODB_URI, JWT_SECRET, JWT_REFRESH_SECRET
#
# name: CI
# on:
#   push:
#     branches: [main]
#   pull_request:
#     branches: [main]
# jobs:
#   lint-and-build:
#     name: Lint & Build
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '20'
#           cache: 'npm'
#       - name: Install dependencies
#         run: npm ci
#       - name: Lint
#         run: npm run lint
#       - name: Build packages
#         run: npm run build --workspace=packages/schemas --workspace=packages/types
#       - name: Build API
#         run: npm run build --workspace=apps/api
#       - name: Build Web
#         run: npm run build --workspace=apps/web
#         env:
#           NEXT_PUBLIC_API_URL: http://localhost:3001/api/v1
#           NEXT_PUBLIC_APP_URL: http://localhost:3000
